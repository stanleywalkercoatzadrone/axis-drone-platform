import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.axisdroneplatform.app',
  appName: 'Axis Drone Platform',
  webDir: 'dist'
};

export default config;
