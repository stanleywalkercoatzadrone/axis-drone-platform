# Environment Variables

# Backend API
VITE_API_URL=http://localhost:8080/api
VITE_SOCKET_URL=http://localhost:8080

# Gemini API
API_KEY=your_gemini_api_key_here

# Google OAuth (for Drive integration)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback

# Database - Option 1: Supabase (Recommended for Production)
# Uncomment and use this for Supabase/managed PostgreSQL
# DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Database - Option 2: Local PostgreSQL (Development)
# Comment these out if using DATABASE_URL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=skylens_db
DB_USER=postgres
DB_PASSWORD=postgres

# Supabase (Optional - for Storage and future features)
# Get these from: Supabase Dashboard → Project Settings → API
# SUPABASE_URL=https://[project-ref].supabase.co
# SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_KEY=your_supabase_service_role_key
# SUPABASE_STORAGE_BUCKET=drone-images

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Storage Configuration
# Options: 'local', 's3', 'supabase', or 'gcs'
STORAGE_PROVIDER=local

# Google Cloud Storage (Optional - only if using GCS storage)
# GCS_PROJECT_ID=your-gcp-project-id
# GCS_BUCKET_NAME=skylens-drone-images
# GOOGLE_APPLICATION_CREDENTIALS=./skylens-sa-key.json

# AWS S3 (Optional - only if using S3 storage)
# USE_S3=false
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=skylens-images

# Server
BACKEND_PORT=8080
FRONTEND_URL=http://localhost:3000
NODE_ENV=development
